apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: be-rust
spec:
  strategy:
    type: Docker
  output:
    to:
      name: 'ocp-demo:rust'
      kind: ImageStreamTag
  runPolicy: Serial
  source:
    type: Git
    contextDir: /be-rust
    git:
      ref: main
      uri: 'https://github.com/mancubus77/ocp-demo-monorepo'
  triggers:
  - type: ConfigChange
